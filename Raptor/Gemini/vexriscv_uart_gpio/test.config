#test_owner: "Bilal Ahmed"

#test_details: "This test performs the write operation on specific addresses and the data is passed to GPIO OUT, which is mapped to the LEDs on the board"

#test_status: "active"

#dependent_software: "raptor", "vcs", "verilator", "litex"
